package com.eval1.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import com.eval1.models.VTotalRembourses;

public interface VTotalRemboursesRepo extends JpaRepository<VTotalRembourses, Long> {
}
