package com.eval1.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import com.eval1.models.VBenefice;

public interface VBeneficeRepo extends JpaRepository<VBenefice, Long> {
}
